services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: chat
      POSTGRES_PASSWORD: chat
      POSTGRES_DB: chat
    ports:
      - "5432:5432"
    volumes:
      - ./db/init.sql:/docker-entrypoint-init.d/init.sql
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U chat"]
      interval: 5s
      timeout: 5s
      retries: 5

  server:
    build: ./server
    ports:
      - "8080:8080"
    environment:
      DATABASE_URL: "host=postgres user=chat password=chat dbname=chat"
    depends_on:
      postgres:
        condition: service_healthy
  client:
    build: ./client
    environment:
      SERVER_URL: "http://server:5432"
    depends_on:
      - server
    stdin_open: true
    tty: true

volumes:
  pgdata: