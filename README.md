#
- start all project `docker compose up --build`
- start PostgreSQL `docker compose up --build postgres`
- start server `docker compose up --build server`
- start client `docker compose up --build client`
